# 채점 기준 (P2 에서 동결)

이 문서는 골든셋(`data/goldenset.json`) 문항을 채점하기 전에 정한다. 답변을
한 건도 보기 전에 정하는 것이 핵심이다. 결과를 보고 기준을 정하면 좋은
점수가 나오는 쪽으로 기준이 휜다.

## 점수 척도

- 1.0 — 정답과 실질적으로 동일. `answer_aliases`에 적은 표기 차이는 허용한다.
- 0.5 — 부분적으로 맞음. 집합형에서 일부만 맞혔거나, 정답을 포함하나
  근거 없는 오답도 함께 제시한 경우.
- 0.0 — 틀림 또는 근거 없는 내용.

## 별칭 인정 범위

문항의 `answer_aliases`에 미리 적은 표기만 인정한다. 채점 중에 새로
추가하지 않는다.

## 집합형(`answer_type: "set"`) 부분 점수

문항마다 `partial_credit`에 `{"full": N, "half": M}`을 적는다.
- 원소를 N개 이상 맞히면 1.0
- M개 이상이면 0.5
- 그 아래는 0.0

## 답변 불가(`answer_type: "none"`) 문항

에이전트가 `decision == "abstain"`이고 `llm_answer_called == false`이면 1.0.
구체적 답을 지어내면 0.0. 기대 동작은 `expected_behavior`와 `abstain_reason`이
담고, `answer` 문자열(항상 `null`)과 비교하지 않는다.

`abstain_reason`은 두 가지를 구분한다.
- `out_of_scope` — 도메인 밖 질문. 라우터가 막아야 한다.
- `no_supported_path` — 도메인 안이지만 근거가 없는 질문. 증거 게이트가 막아야 한다.

## 경로 재현율

`expected_paths`가 복수일 수 있다(대안 경로). 채점은 각 대안에 대해 접두
재현율을 구하고 최댓값을 취한다. 어느 대안이 선택됐는지는 `matched_path_index`로
기록해 파생 간선(예: COVERED)이 실제로 지름길을 만들었는지 별도로 집계한다.

## 변경

기준을 바꿔야 할 사정이 생기면 바꾸되, 바꾼 사실과 이유를 REPORT.md에 적고
바꾸기 전후 점수를 함께 싣는다. 조용히 고치지 않는다.

## 세대 교차(`era_cross`) 문항 수에 대한 기록

설계서는 세대 교차 문항 4건을 목표로 했다. 실제 코퍼스(52건)에서 검증
가능한 세대 교차 사실(원문에 실재하는 인용문으로 뒷받침되는 것)은 2건뿐이었다.

- Q06 — 빅뱅(2008, 2000s)이 이문세(1988, 1980s 이전)의 곡을 리메이크한 사실
- Q16 — 양현석(1990s, 서태지와 아이들 멤버)이 세운 YG에 빅뱅(2006년 결성, 2000s)이
  소속된 사실

나머지는 만들 수 없어서가 아니라, 만들려면 코퍼스에 없는 사실을 지어내야
했기 때문에 만들지 않았다. 이 격차(설계 4건 대 실측 2건)를 REPORT.md의
골든셋 절에 그대로 적는다.

## P5 관문 골든셋 경로 실현율에 대한 기록

Task 18~19 에서 그래프 정규화 버그 세 건(COVERED 파생 무력화 · Era 노드
오염 · Song/Album 맥락 분열)과 추출 설정 버그 두 건(max_doc_chars=6000
이 52건 중 25건을 잘라냄 · AWARD_CANON 의 'Mnet' 영문 표기 누락)을
실측으로 찾아 고쳤다. 그 결과 골든셋 경로 실현율은 32% -> 45% -> 59%
-> 73%(16/22)로 올랐다.

남은 6건(Q07·Q08·Q09·Q17·Q18·Q19)은 파이프라인 버그가 아니라 실제
LLM 추출 누락으로 확인했다.

- Q07·Q09·Q17·Q18·Q19 — 박진영이 다른 그룹(god, Stray Kids, DAY6) 문서
  안에서 부차적으로만 언급된다. 근거 문장("싱어송라이터이자 JYP
  엔터테인먼트의 설립자인 박진영을...")이 6000자 상한 이전 구간(920번째
  글자)에 있어 자르기 문제가 아님을 확인했다. LLM 은 이 문장에서
  INFLUENCED 관계만 추출했고 FOUNDED·PRODUCED 는 세 번의 전체 재추출
  모두에서 한 번도 만들지 않았다. 문서 주인공이 아닌 인물의 부차적
  사실에 대한 LLM 재현율 한계로 판단한다.
- Q08 — "난 알아요"가 언급된 문장("그 때의 미디 작업으로 〈난 알아요〉,
  〈환상속의 그대〉등 서태지와 아이들 1집에 수록될 곡들의 일부가
  만들어진다.")도 상한 이전 구간(301번째 글자)에 있다. 앨범명을
  "서태지와 아이들 1집"으로 에둘러 표현해 정규 앨범 표제("Seotaiji and
  Boys")와 문자열이 다른 것도 CONTAINS 미추출의 한 원인으로 보인다.

세 번째 전체 재추출(청크 125->221개, 실제 과금)까지 진행한 뒤 이 상태를
동결한다. 더 파는 것은 비용 대비 회수가 작다고 판단했다(사용자 확인).
남은 6건은 실패 층 분류에서 "index"(그래프에 사실 자체가 없음) 로
기록하고, REPORT.md 에 그대로 남긴다.

## 심판 프로바이더 변경 기록

설계서 6.3 은 "보유 키가 OpenAI·Google 둘뿐이므로 답변은 OpenAI, 심판은
Google로 가른다. 같은 모델이면 자기 문체를 편애한다"고 못박았다. P8
평가를 처음 실행하던 중 Google `gemini-3.8-flash` 키가 무료 티어의
일일 한도(20건)에 걸려 심판을 한 건도 더 부를 수 없었다(튜닝 15문항 ×
답변 출처 2곳 × 반복 3회 = 90회가 필요했는데 최초 시도만으로 소진).
한도 리셋을 기다리지 않고 심판을 OpenAI `gpt-4.1-mini`로 바꿔 진행하기로
사용자와 합의했다.

이 변경은 자기평가 편향 방지라는 설계 원칙과 정면으로 부딪힌다 — 그래프
답변(`gpt-4.1`)과 심판(`gpt-4.1-mini`)이 이제 같은 회사 모델이다. 완전한
회피는 아니지만, 최소한 같은 모델을 쓰지는 않도록(크기가 다른 모델)
부분적으로 완화했다. 이 한계를 REPORT.md 에 그대로 적는다 — 숨기지
않는다. `judge_repeats` 도 3에서 1로 낮췄다(호출 수를 줄이기 위함이며,
이는 Google 한도와 무관하게 OpenAI 비용도 아끼려는 목적이 크다). 표준편차
보고(설계서 6.3의 "3회 반복해 평균과 표준편차를 함께 보고")는 이번
튜닝 평가에서는 생략됐다는 뜻이고, 이 역시 REPORT.md 에 명시한다.
